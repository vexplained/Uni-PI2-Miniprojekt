# Generated by Gemini -> see Chat from 22.05.2025

# Eclipse specific files
.metadata/
*.launch
*.history

# Project-specific Eclipse configuration files
# These are generally good to commit for consistent project setup across developers.
# If these files cause issues due to local paths or sensitive data, you might reconsider.
#.project
#.classpath

# Eclipse .settings/ directory:
# Ignore the entire .settings/ directory first, then un-ignore specific useful files.
.settings/* # specifically exclude files in settings folder because otherwise couldn't un-ignore them. See https://stackoverflow.com/a/47994523

# == Un-ignore common and sensible settings from .settings/ ==
# Code Style and Formatter Settings: Essential for consistent code appearance.
!.settings/org.eclipse.jdt.core.prefs       # Java compiler, compliance, and other core settings (often includes code style profiles)
!.settings/org.eclipse.jdt.ui.prefs         # Java UI preferences, including formatter settings and save actions
!.settings/org.eclipse.core.resources.prefs # Workspace preferences (e.g., text file encoding)

# Other potentially useful settings to share:
# !.settings/org.eclipse.wst.jsdt.ui.prefs # JavaScript formatter preferences (if applicable)
# !.settings/org.eclipse.wst.html.ui.prefs # HTML formatter preferences (if applicable)
# !.settings/org.eclipse.m2e.core.prefs    # Maven specific settings for Eclipse
# !.settings/org.eclipse.debug.core.prefs  # Debugger settings if shared launch configurations are defined in project
# !.settings/org.eclipse.buildship.core.prefs # Gradle specific settings for Eclipse

# Binaries and build artifacts (for Maven/Gradle projects)
target/
build/
bin/
*.jar
*.war
*.ear
*.class

# Maven/Gradle generated files
.gradle/
.idea/ # If any developers use IntelliJ IDEA
*.iml # If any developers use IntelliJ IDEA
*.ipr
*.iws
out/

# Log files
*.log
logs/

# Temporary files
tmp/
*.tmp
*.bak
*~.nib
*.swp
*.orig

# Operating System generated files
.DS_Store # Mac
Thumbs.db # Windows

# Local configuration files (should not be committed)
# If you have local configuration files that might contain sensitive data or
# are specific to a developer's setup, add them here. For example:
# src/main/resources/application-dev.properties
# config/local.properties